import { HashpasswordDirective } from './hashpassword.directive';

describe('HashpasswordDirective', () => {
  it('should create an instance', () => {
    const directive = new HashpasswordDirective();
    expect(directive).toBeTruthy();
  });
});
